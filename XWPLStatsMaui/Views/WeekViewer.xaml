<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:XWPLStats.Models"
             xmlns:viewmodels="clr-namespace:XWPLStats.ViewModels"
             xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:DataType="viewmodels:WeekViewerViewModel"
             x:Class="XWPLStats.Views.WeekViewer"
             Title="All Weeks"
                 BackgroundColor="{AppThemeBinding 
                    Dark={StaticResource WindowBackgroundColorDark}, 
                    Light={StaticResource WindowBackgroundColor}}">
    <ContentPage.BindingContext>
        <viewmodels:WeekViewerViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <mct:SelectedItemEventArgsConverter x:Key="SelectedItemEventArgsConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ListView
        IsRefreshing="{Binding RefreshCommand.IsRunning, Mode=OneWay}"
        ItemsSource="{Binding WeeksFull}"
        RefreshCommand="{Binding RefreshCommand}"
        IsPullToRefreshEnabled="True"
        SelectionMode="None"
        Style="{StaticResource PlayerListView}"
        CachingStrategy="RecycleElement"
        x:Name="WeeksList">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:WeekFullInfo">
                <ViewCell>
                    <Grid Padding="3">
                        <Frame Style="{StaticResource PlayerCard}">
                            <StackLayout Orientation="Vertical">
                                <StackLayout VerticalOptions="Center">
                                    <Grid Padding=" 3" RowSpacing="5" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="{Binding TeamName, StringFormat='Team Played:   {0}'}"
                                            VerticalOptions="Center"
                                            Grid.Column="0"
                                            Grid.ColumnSpan="2"
                                            Grid.Row="0" />
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="{Binding WeekNumber, StringFormat='Week #: {0}'}"
                                            VerticalOptions="Center"
                                            Grid.Column="0"
                                            Grid.Row="1" />
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="WeekWon:"
                                            HorizontalOptions="Start"
                                            VerticalOptions="Center"
                                            Grid.Column="1"
                                            Grid.Row="4" />
                                        <CheckBox IsChecked="{Binding WeekWon}"
                                                  Style="{StaticResource CheckboxStyle}"
                                                  Grid.Column="1"
                                                  Grid.Row="4"
                                                  HorizontalOptions="End"
                                                  IsEnabled="False" />
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="{Binding GamesWon, StringFormat='Games Won: {0}'}"
                                            VerticalOptions="Center"
                                            Grid.Column="0"
                                            Grid.Row="2" />
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="{Binding GamesLost, StringFormat='Games Lost: {0}'}"
                                            VerticalOptions="Center"
                                            Grid.Column="0"
                                            Grid.Row="3" />
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="{Binding Average, StringFormat='Average: {0}%'}"
                                            VerticalOptions="Center"
                                            Grid.Column="1"
                                            Grid.RowSpan="2"
                                            Grid.Row="2" />
                                        
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="{Binding DatePlayed, StringFormat='Date: {0}'}"
                                            VerticalOptions="Center"
                                            Grid.Column="0"
                                            Grid.Row="4" />
                                        <Label
                                            Style="{StaticResource LabelSmall}"
                                            Text="Home:"
                                            HorizontalOptions="Start"
                                            VerticalOptions="Center"
                                            Grid.Column="1"
                                            Grid.Row="1" />
                                        <CheckBox IsChecked="{Binding Home}"
                                                  Style="{StaticResource CheckboxStyle}"
                                                  Grid.Column="1"
                                                  Grid.Row="1"
                                                  HorizontalOptions="End"
                                                  IsEnabled="False" />
                                    </Grid>
                                </StackLayout>
                            </StackLayout>
                        </Frame>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>